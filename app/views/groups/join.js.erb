<%if request.referrer.include?("followings")%>
	$("#joinGroup_<%=@group.id%>").text("Joined").attr("href","#-").removeAttr("data-remote");
<%else%>
	$("#joinGroup_<%=@group.id%>").text("Joined");
	$("#group_<%=@group.id%>").fadeOut(2000);
	setTimeout(
	  function() 
	  {
		$(".grpOuter.suggest_groups").html("<%= j render('users/shared/suggested_groups', suggest: @suggest)%>");
	  }, 2000);
<%end%>