<div class="clearfix whiteBox">
  <div class="clearfix formOuterPost tal">
    <div class="col-sm-12 mb10">
      <div class="grayHD">Add category </div>
    </div>
    <div class="col-sm-12 addSkills">
      <label class="font18">Select Category </label><br>
    </div>
    <%=form_tag category_profiles_path, method: true do%>
      <%Category.all.each_with_index do |category, index|%>
        <%#if index.even?%>
          <div class="col-sm-6 pr10 categoriesChk">
          <%if current_user.users_category.present? && current_user.users_category.category_ids.include?(category.id.to_s)%>
            <%= check_box_tag 'category_ids[]', category.id,:checked=>true%> <%=category.try(:name)%>
          <%else%>
            <%= check_box_tag 'category_ids[]', category.id%> <%=category.try(:name)%>
          <%end%>
            <%#= check_box_tag 'category_ids[]'%> <%#=category.try(:name)%>
          </div>
        <%#else%>
          <div class="col-sm-6 pl10 categoriesChk">
             <%#= check_box_tag 'category_ids', category.try(:id)%><%#=category.try(:name)%>
          </div>
        <%#end%>
      <%end%>    
      <div class="clearfix"></div>
      <div class="col-sm-12 mt10">
        <%= submit_tag "Save", class: 'blueBtn' %>
        <a href="#" class="grayBtn">Cancel</a> 
      </div>
    <%end%>  
  </div>
</div>