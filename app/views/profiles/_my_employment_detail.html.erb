<div class="clearfix whiteBox">
  <div class="clearfix formOuterPost tal">
    <div class="col-sm-12 mb10">
      <div class="grayHD">EMPLOYMENT DETAILS
        <%# if @user==current_user %>
         <a href="/profiles/add_employment" class="blueBtn fr addBtn" data-remote=true> &nbsp;&nbsp; Add Employment</a>
        <%#end%>
      </div>
      <% current_user.employment_details.each do |employment| %>
        <div class="uniOuter">
          <div class="blackHD"><%= employment.designation %>
            <img src="/assets/images/uni1.jpg" alt="" class="fr">
          </div>
          <div class="blackColor">
            <%= employment.organization %>
            <div class="grayColor">            
              <%= employment.start_work_date.to_date.strftime("%b %Y") rescue nil %> – <%= employment.worked_till.to_date.strftime("%b %Y") rescue nil %>
            </div>
            <div class="mt20 blackColor borderBtm">
              <p><%= employment.try(:description)%></p>
            </div>
          </div>
        </div>
      <% end if current_user.employment_details%>
    </div>
    <!-- Render Employment Details -->
    <div class="employment_detail"></div>
  </div>
</div>