<div class="mainCOntnt">
  <%=render "layouts/left_panel"%>
  <div class="rightContent">
    <%=render 'layouts/topNav'%>
    <div class="clearfix innerWIdth550 tac ">
      <div class="clearfix whiteBox mt30 profile">        
        <%=render 'profile'%>
      </div>  
      <div id="education_history"><%=render 'my_education_history'%></div>
      <div class="employment-box"><%=render 'my_employment_detail'%></div>
      <!-- Render Skills -->
      <div class="my_skillsTag">
        <%=render 'my_skill'%>
      </div>
      <!-- Render Category -->
      <div class="my_category">
        <%=render 'my_category'%>
      </div>
    </div>    
  </div>
</div>