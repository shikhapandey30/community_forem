<%if params[:type]=='User'%>
  <%@data_json = User.all.map{|u| {:value=> u.id,:label=>u.try(:screen_name)}}%>
<%else%>
  <%@data_json = Post.all.map{|u| {:value=> u.id,:label=>u.post_text}}%>
 <%end%>
