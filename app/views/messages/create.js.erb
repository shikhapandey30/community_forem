<%if @archive%>
	$('.myConnection').html("<%= j render('my_connections') %>");
<%end%>
$(".replyBox textarea").val('');

<%# publish_to "/messages/sender_#{current_user.id}" do %>
  $("#chatmsg").append("<%= j render('message', msg: @message) %>");
<%# end %>
<% publish_to "/messages/reciever_#{@friend.id}" do %>	
  $("#chatmsg").append("<%= j render('reciever_msg', msg: @message) %>");
  $('#chatmsg').children().last()[0].scrollIntoView(true);
<% end %>

$('#chatmsg').children().last()[0].scrollIntoView(true);